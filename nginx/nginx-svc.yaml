apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    run: nginx
  namespace:
spec:
  ports:
  - protocol: TCP
    port: 90
    targetPort: 80
  selector:
    run: nginx
  type: ClusterIP

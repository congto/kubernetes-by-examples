apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: minio
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  - host: cmp.local
    http:
      paths:
      - path: /
        backend:
          serviceName: minio
          servicePort: 9000

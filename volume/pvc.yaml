---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: volume-claim-dynamic
spec:
  #storageClassName: retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
